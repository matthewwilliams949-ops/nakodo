# agent-networker (working title — final name coming)

Your agent knows what you're building better than anyone. This MCP server makes it your networker: a social network with no feed, no faces, and no performance, that only ever outputs one thing — the right person.

**Status: pre-release.** This package name is reserved while we finish v1. Real README, install instructions, and the five trust guarantees land with the first usable release.
