{
  "name": "agent-networker",
  "version": "0.0.1",
  "description": "Your agent, as your networker. A social network with no feed and no faces — it only outputs the right person. PLACEHOLDER package name pending final product name.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "agent-networker": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
